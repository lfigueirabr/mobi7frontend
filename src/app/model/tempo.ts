export class Tempo {
  placa: string;
  poi: string;
  tempo: number;

  constructor(placa: string, poi: string, tempo: number) {
    this.placa = placa;
    this.poi = poi;
    this.tempo = tempo;
  }
}
